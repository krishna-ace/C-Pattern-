#include <stdio.h>
int main() {
    int n = 5;
    for (int i = 0; i < n; i++) {
        for (int space = 0; space < n - i - 1; space++)
            printf(" ");
        for (int star = 0; star < 2 * i + 1; star++)
            printf("*"); 
        printf("\n");
    }
    for (int i = n - 2; i >= 0; i--) {
        for (int space = 0; space < n - i - 1; space++)
            printf(" ");
        
        for (int star = 0; star < 2 * i + 1; star++)
            printf("*");   
        printf("\n");
    }
    return 0;
}
